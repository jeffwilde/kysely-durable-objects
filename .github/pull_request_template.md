## Summary

<!-- What changes and why -->

## Test plan

- [ ] `npm test` passes locally (workerd suite)
- [ ] If touching the dialect, added/updated a workerd test that exercises the change against real `ctx.storage.sql`
- [ ] Updated README if behavior or limitations changed
